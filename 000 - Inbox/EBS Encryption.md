It makes everything encrypted, e.g. snapshots, data at rest, data in transit.

it has nearly no effect on latency and performance, and considered a good practice to always do it.

to encrypt a non encrypted volume you should use snapshots and create an encrypted disk from it.
